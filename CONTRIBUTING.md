# Contributing

Before submitting a change:

1. Read `docs/architecture.md` and the relevant contract documentation.
2. Register new capabilities instead of modifying core dispatch logic.
3. Add fixtures and verification for every supported target or stack combination.
4. Run `pnpm check`.

Contributions must include tests and use conventional, focused commits.
